# 新手小白搭建个人健身系统指南

这份指南写给第一次接触本地 Agent、不会写代码，也没有系统训练记录的人。你不需要理解脚本、JSON 或 Obsidian 链接。正常情况下，只要准备好一台 Windows 电脑和一个能够操作本地文件的 AI Agent，就可以让 AI 帮你完成安装和搭建。

## 一、你最后会得到什么

搭建完成后，你会得到一套保存在自己电脑里的个人健身系统，主要包括：

- 一份根据你的目标、时间、器械和身体情况生成的完整训练计划；
- 一个可以直接双击打开的 `健身工作台.html`；
- 每次训练后的复盘记录；
- AI 根据真实训练反馈给出的下一次训练安排；
- 停训、漏练或状态变化后的接回方案；
- 可以整体复制到新电脑的本地文件夹。

它不是一个替你训练的软件。你负责真实训练和如实反馈，AI 负责整理信息、生成计划、记录复盘和更新下一次安排。

## 二、开始前准备

你需要准备：

1. 一台 Windows 电脑；
2. 一个能够读取和修改本地文件的 AI Agent，例如 Codex、Claude Code，或支持本地 Skill 的兼容 Agent；
3. Python 3.10 或更新版本；
4. 第一次下载项目时需要联网，系统搭建完成后，主要页面可以离线打开；
5. 一个准备存放健身系统的空文件夹，例如在“文档”中新建名为“我的健身系统”的文件夹。

Obsidian 不是必需品。没有安装 Obsidian，也可以在浏览器里打开工作台、训练计划和复盘内容。

如果你不知道电脑有没有 Python，可以把下面这句话交给本地 AI：

```text
请检查这台 Windows 电脑是否安装了 Python 3.10 或更新版本。如果没有，请告诉我最简单、安全的安装方法；安装完成后再继续，不要跳过版本检查。
```

## 三、最推荐的搭建方法：让 AI 全程操作

### 第 1 步：把安装任务交给本地 AI

打开本地 AI Agent，新建一个任务，把下面这段话发给它：

```text
请从 GitHub 下载 LZheng0411/Lzheng-fitness 的最新正式版本，阅读项目 README 和 AGENTS.md，检查 Python 版本，安装全部 7 个 Lzheng Fitness Skills，并运行项目自带的完整验证。不要覆盖我电脑上已有的同名 Skill；如果发现旧版本，先告诉我备份和升级方案。完成后告诉我安装位置、验证结果，以及我下一步只需要说什么。
```

项目地址：<https://github.com/LZheng0411/Lzheng-fitness>

AI 应该完成以下工作：

1. 下载项目最新正式版本；
2. 确认 Python 版本符合要求；
3. 安装全部 7 个 Skill；
4. 运行完整验证；
5. 明确告诉你安装是否成功，而不是只说“应该可以”。

### 第 2 步：重新开一个 AI 对话

安装成功后，建议新建一个对话。这样可以确保 AI 重新加载刚安装的 Skill。

你只需要说：

```text
开始建立我的健身系统。
```

正常情况下，AI 会先问你：

- 主要目标是增肌、减脂、提升力量，还是综合改善；
- 你是完全新手、练过一段时间，还是已经有稳定计划；
- 有没有疼痛、旧伤、术后恢复或其他会影响训练的情况。

如果 AI 一上来让你自己找脚本、填写 JSON 或研究 README，说明它没有按新手入口工作。先新开对话再说一次“开始建立我的健身系统”。

### 第 3 步：完成个人建档

接下来，AI 会继续了解会真正影响训练计划的信息：

- 每周能训练几次、每次多长时间；
- 在家还是健身房训练，有哪些器械；
- 最近是否训练过，以及过去做过哪些动作；
- 睡眠、疲劳、工作压力和恢复情况；
- 身高、体重及你愿意提供的身体变化记录；
- 目前可靠的动作重量和训练记录。

不知道的内容可以直接说“不知道”。不要为了让资料看起来完整而猜重量、猜次数或隐瞒疼痛。

### 第 4 步：完成重量校准

如果你没有可靠的训练重量，系统不应该直接编出一个重量，也不应该让你随便猜。

AI 会带你用较轻重量试做，并根据以下反馈逐步找到起始重量：

- 实际使用的重量；
- 完成的次数；
- 做完后大约还能再做几次；
- 动作是否稳定；
- 有没有疼痛或异常不适；
- 主观感觉偏轻、合适还是偏重。

重量校准不是测试极限。新手第一次训练的重点，是学会动作并找到可以稳定重复的起点。

### 第 5 步：确认第一版正式计划

建档和必要的重量校准完成后，AI 应先向你展示计划依据，再生成正式文件。你至少要看懂：

- 计划的主要目标；
- 每周训练几次；
- 每个训练日练什么；
- 第一次训练做哪些动作；
- 时间不足时怎么缩短；
- 出现什么情况应该停止；
- 哪些重量已经确认，哪些还需要现场校准。

如果训练频率、动作、周期或目标与你确认的信息不一致，先让 AI 修改，不要带着明显错误开始执行。

### 第 6 步：打开健身工作台

搭建完成后，AI 应告诉你系统文件夹和当前正式入口。通常打开：

```text
个人训练系统\健身工作台.html
```

直接双击这个文件，就会用浏览器打开工作台。日常主要看这里，不需要进入代码目录。

你需要检查：

- 页面能否正常打开；
- 背景图片或视频是否显示；
- “完整训练计划”按钮能否打开；
- 当前训练安排是否与你刚确认的计划一致；
- 页面在缩小窗口后有没有横向溢出或内容遮挡。

示例数据只用于证明系统能够运行，不是你的真实训练处方。正式使用前，工作台必须接入你的计划和确认过的训练事实。

## 四、搭建成功后怎么使用

你不需要每天操作文件，也不需要记住 Skill 名称。直接用自然语言告诉 AI 发生了什么。

### 训练前

```text
我今天练什么？请根据当前计划告诉我动作、重量、组数、次数、休息时间和时间不足版本。
```

### 训练后

```text
我练完了。请根据我接下来提供的实际重量、次数、余力、疼痛和主观感受完成复盘，并给出下一次明确安排，同时刷新工作台。
```

### 一周复盘

```text
帮我复盘这一周的训练。区分已经验证的事实和还不确定的判断，先告诉我是否需要调整计划，再更新下周安排。
```

### 停训或连续漏练

```text
我已经一周没有训练了，请不要让我补课。先判断我现在适合正常接回、降级训练还是最低任务，再生成未来 7 天的接回安排并刷新工作台。
```

### 想提高某个力量动作

```text
我想提高卧推。请先读取我的当前完整计划和近期训练，不要让专项周期与原计划冲突；需要修改原计划时先展示差异，等我确认。
```

每次训练后，尽量提供真实的重量、组数、次数、余力、疼痛和主观感受。信息不完整时，AI 应该追问，不应该把猜测写成事实。

## 五、怎么替换工作台壁纸

准备好一张 PNG、JPEG 或 WebP 图片。如果想使用动态背景，再准备一个 MP4 视频。然后告诉 AI：

```text
请使用 Lzheng Fitness 项目自带的壁纸替换功能，把这张图片设为我的健身工作台背景。先自动备份原背景，替换后检查电脑端和手机端显示，失败时恢复原版本，不要手工修改 HTML。
```

动态背景可以这样说：

```text
请把这个 MP4 设为动态背景，并把这张图片作为视频加载失败时的后备背景。分别检查电脑端和手机端取景，不要手工修改 HTML。
```

壁纸功能会自动备份旧背景。只使用图片时，系统会关闭原来的背景视频。

## 六、迁移到新电脑

迁移时不要只复制 `健身工作台.html`。单独复制一个 HTML，会丢失计划、复盘、背景素材和系统配置。

正确方法：

1. 复制整个健身系统根目录；
2. 在新电脑安装 Python 和本地 AI Agent；
3. 在新电脑重新安装最新的 7 个 Lzheng Fitness Skills；
4. 把复制过来的完整系统目录交给 AI；
5. 让 AI 运行迁移检查和完整验证；
6. 再打开迁移后目录中的 `个人训练系统\健身工作台.html`。

可以直接对新电脑的 AI 说：

```text
这是我从旧电脑完整复制过来的 Lzheng 健身系统。请安装或检查最新的 7 个 Skill，以这个目录的当前位置为准运行迁移诊断和完整验证，修复旧的绝对路径，但不要覆盖我的计划、复盘、状态档案、私人资料和自定义壁纸。完成后逐项测试工作台、完整计划、复盘阅读和背景素材能否打开。
```

目录换盘符、改名字或放进带中文和空格的路径都可以，但必须整套目录一起移动。Obsidian 只提供可选编辑入口，不应该成为浏览器打开工作台的前提。

## 七、常见问题

### 1. AI 说找不到新安装的 Skill

先新建一个 AI 对话。如果仍然找不到，让 AI 检查 Skill 是否安装到了当前 Agent 实际使用的目录，而不是重复下载项目。

### 2. 安装时提示同名 Skill 已存在

不要直接强制覆盖。让 AI 先比较版本并备份旧目录，再决定升级。正式计划、复盘、状态档案、私人知识和自定义壁纸都不应该被升级覆盖。

### 3. 双击工作台后页面打不开或按钮失效

先确认你移动的是完整系统目录，而不是单独的 HTML。让 AI 对当前系统根目录运行 `doctor` 和 `validate`，并检查相对链接。不要把链接改成某台电脑专用的绝对路径。

### 4. Obsidian 显示 Vault not found

这不应该影响主要功能。先在浏览器中使用工作台内置阅读入口。需要用 Obsidian 编辑时，再让 AI 根据当前电脑和当前目录重新生成可选编辑入口，不要沿用旧电脑的 vault 路径。

### 5. 工作台打开了，但显示的是示例内容

示例内容只代表初始化成功。告诉 AI 完成正式建档、重量校准和计划生成，并把正式计划接入当前周期、执行基准、复盘索引和工作台。

### 6. AI 修改了计划，却没有更新工作台

直接要求它检查“当前计划、执行基准、复盘、状态档案和工作台是否一致”。正式更新完成后，AI 应明确列出创建、更新、保持不变和失败的文件。

### 7. 更换电脑后壁纸丢失

通常是因为只复制了 HTML，或者背景文件放在系统目录外。让 AI 使用项目自带的壁纸替换功能重新接入素材，然后再次整目录迁移验证。

## 八、怎样判断系统真的搭建成功

下面这些条件全部满足，才算完成：

- [ ] 7 个 Skill 已安装，完整验证通过；
- [ ] AI 已完成个人建档，并明确区分已知信息和未知信息；
- [ ] 不可靠的动作重量没有被猜测成正式处方；
- [ ] 第一版正式训练计划已经生成；
- [ ] `健身工作台.html` 可以直接用浏览器打开；
- [ ] 工作台中的完整计划、复盘和状态内容能够读取；
- [ ] 当前计划、执行基准、复盘和工作台内容一致；
- [ ] 工作台在电脑端和手机端宽度下都能正常显示；
- [ ] 整个系统目录移动或改名后，主要入口仍然能够打开；
- [ ] 你知道训练前、训练后和停训后分别应该对 AI 说什么。

## 九、安全边界

这套系统提供一般训练规划、记录和调整支持，不代替医疗诊断或康复治疗。

出现胸部异常不适、晕厥、异常气短、锐痛、麻木、放射痛、明显功能受限或症状持续加重时，应停止普通高强度训练并寻求合适的专业评估。疼痛不是“意志力不够”，也不能只用训练强度评分来解释。

## 十、只记住这三句话

如果你不想记住整份指南，只需要记住：

1. 安装后说：`开始建立我的健身系统。`
2. 训练前说：`我今天练什么？`
3. 训练后说：`我练完了，这是我的真实训练数据，请复盘并更新下一次安排和工作台。`

你负责真实执行，AI 负责把每一次真实训练变成下一次更明确的行动。
