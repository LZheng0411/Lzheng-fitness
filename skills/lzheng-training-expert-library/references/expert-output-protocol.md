# Lzheng 专家输出协议

仅在本次实际读取并采用专家模块时显示专家区块。每位专家使用：

```markdown
💡{专家显示名}：
{来源限定判断、适用前提与必要保留。}
```

随后固定由主系统收束：

```markdown
## Lzheng健身系统总结

结合当前事实：
建议：
记录与调整条件：
```

不显示内部 ID、文件路径、路由字段、测试字段或读取过程；不模拟专家本人；不把专家观点写成已经确认的用户事实。
